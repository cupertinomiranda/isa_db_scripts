<% 
require 'rubygems'
load 'init.rb'

Relocation.each do |reloc| 
%>
ARC_RELOC_HOWTO(<%= reloc.name.slice(2, 200) %>, <%= reloc.value  %>, \
                <%= reloc.replacement_mask.size_for_reloc %>, \
                <%= reloc.replacement_mask.bitsize %>, \
                replace_<%= reloc.type_name %>, \
                <%= reloc.complain_overflow %>, \
                <%= reloc.calc.to_c %>)
<% end %>
